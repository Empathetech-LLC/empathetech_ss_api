name: empathetech_ss_api
description: Firebase API code for Empathetech's Smoke Signal
version: 1.0.0
repository: https://github.com/Empathetech-LLC/empathetech_ss_api

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase (Google)
  firebase_core: ^2.3.0
  firebase_auth: ^4.1.4
  firebase_messaging: ^14.1.2
  cloud_firestore: ^4.1.0
  cloud_functions: ^4.0.10

  # Community
  cached_network_image: ^3.2.3
  flutter_platform_widgets: ^3.2.0

  # Also Empathetech
  empathetech_flutter_ui: ^1.1.2
